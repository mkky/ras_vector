transforms:
  metrics_session:
    type: log_to_metric
    inputs:
      - filter_cmd.session
    metrics:
      - type: gauge
        field: session_id
        name: session_session_id
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: passive_session_hibernate_time
        name: session_passive_session_hibernate_time
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: hibernate_session_terminate_time
        name: session_hibernate_session_terminate_time
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: blocked_by_dbms
        name: session_blocked_by_dbms
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: blocked_by_ls
        name: session_blocked_by_ls
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: bytes_all
        name: session_bytes_all
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: bytes_last_5min
        name: session_bytes_last_5min
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: calls_all
        name: session_calls_all
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: calls_last_5min
        name: session_calls_last_5min
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: dbms_bytes_all
        name: session_dbms_bytes_all
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: dbms_bytes_last_5min
        name: session_dbms_bytes_last_5min
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: db_proc_took
        name: session_db_proc_took
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: duration_all
        name: session_duration_all
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: duration_all_dbms
        name: session_duration_all_dbms
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: duration_current
        name: session_duration_current
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: duration_current_dbms
        name: session_duration_current_dbms
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: duration_last_5min
        name: session_duration_last_5min
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: duration_last_5min_dbms
        name: session_duration_last_5min_dbms
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: duration_current_service
        name: session_duration_current_service
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: duration_last_5min_service
        name: session_duration_last_5min_service
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: duration_all_service
        name: session_duration_all_service
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: memory_current
        name: session_memory_current
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"


      - type: gauge
        field: memory_total
        name: session_memory_total
        tags:
          event_id: "{{ event_id }}"
          session_id: "{{ session_id }}"
          connection: "{{ connection }}"
          process: "{{ process }}"
          infobase: "{{ infobase }}"
          user_name: "{{ user_name }}"
          host: "{{ host }}"
          app_id: "{{ app_id }}"
          locale: "{{ locale }}"
          client_ip: "{{ client_ip }}"
          host_vector: "{{ host_vector }}"

